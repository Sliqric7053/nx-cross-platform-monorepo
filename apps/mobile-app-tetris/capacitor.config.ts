import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mobile.tetris',
  appName: 'tetris',
  webDir: '../../dist/apps/mobile-app-tetris',
  bundledWebRuntime: false,
};

export default config;
